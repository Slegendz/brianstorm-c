#include<bits/stdc++.h>

using namespace std;

void print (vector<int> &stones){
    stones.erase(stones.begin());
    stones.erase(stones.begin());

    if(stones.empty()){

    }
}


int main(){
    vector<int>stones = {2,2};
    int a = stones.size();
    
    // while(a>1){
    //     a = stones.size();
    // }

    cout << "hel";
        print(stones);
        cout << "hel";
    if(stones.empty()){
        cout << 33;
    }
}